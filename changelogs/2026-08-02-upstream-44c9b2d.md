---
date: 2026-08-02
upstream_repo: https://github.com/obra/superpowers
upstream_version: post-v6.2.0
upstream_sha: 44c9b2d6e889982ac18c27d05a19fefe335194e1
previous_upstream_sha: 3dcbd5c4b48e02263fbf4a3c01e3fe4f81d584d9
ours_from_sha: 5f2b2db
ours_to_sha: 85963f6
---

# Upstream post-v6.2.0 Refresh (README-only)

Tracks `obra/superpowers` up to `44c9b2d`, one commit past the previously adopted `3dcbd5c` (v6.2.0). Upstream has not cut a new tag; `44c9b2d` is the current tip of `upstream/main`.

The single commit removes the "We're Hiring!" recruiting section from upstream's own `README.md`. It touches no skill, command, agent, hook, or script — the entire pack payload is byte-identical to the v6.2.0 state this fork already carries. Adoption is therefore a pointer advance: `changelogs/UPSTREAM_SHA` moves forward and the pack version takes a patch bump so installed copies can tell the two tracking states apart.

## Upstream Commits Reviewed

- `44c9b2d` - `docs: remove the "We're Hiring" section from the README`

## Upstream coverage

The upstream commit changed:

- `README.md`

`git diff 3dcbd5c..44c9b2d` reports `1 file changed, 6 deletions(-)`. No other path in the upstream tree moved.

## Reconciliation decisions

- **Not ported.** This fork's `README.md` is fork-native prose about `superpowers-extended` — install paths, the `/init` workflow, the changelog protocol — and has never carried upstream's recruiting section (`rg -i 'hiring|primeradiant' README.md docs/` returns nothing). There is no corresponding text to delete, so the commit adopts as a no-op on the payload.
- **`UPSTREAM_SHA` advanced anyway.** Leaving the pointer at `3dcbd5c` would make the next refresh re-review a commit already adjudicated here. The pointer records "reviewed up to", not "changed something", so it moves to `44c9b2d`.
- **Patch version bump, not minor.** The previous refresh (`e5665a1`, v6.2.0) took `2.0.0` → `2.1.0` because it carried real payload changes. This one changes no shipped behavior, so `2.1.0` → `2.1.1` across all four manifests that declare a version.

## Files affected

Modified:

- `.claude-plugin/plugin.json` (version `2.1.0` → `2.1.1`)
- `.claude-plugin/marketplace.json` (version `2.1.0` → `2.1.1`)
- `.codex-plugin/plugin.json` (version `2.1.0` → `2.1.1`)
- `.cursor-plugin/plugin.json` (version `2.1.0` → `2.1.1`)
- `changelogs/UPSTREAM_SHA`

New:

- `changelogs/2026-08-02-upstream-44c9b2d.md`

`.agents/plugins/marketplace.json` declares no version field and is unchanged.

## Verification notes

- `claude plugin validate .claude-plugin/plugin.json` passes with the one standing warning that root `CLAUDE.md` is not loaded as plugin context — expected, since `CLAUDE.md` is maintainer-only and deliberately not part of the payload. `claude plugin validate .` (marketplace) passes clean.
- `rg -n 'template/|\.superpowers-extended|workflows/' skills commands agents docs` surfaces no resurrected legacy paths.
- Payload directories (`skills/`, `commands/`, `agents/`) are untouched by this refresh; `git diff --stat` against `5f2b2db` shows only the manifest, changelog, and pointer files above.

## Upstream diff

```diff
diff --git a/README.md b/README.md
index bb398c6..2757b2e 100644
--- a/README.md
+++ b/README.md
@@ -3,12 +3,6 @@
 Superpowers is a complete software development methodology for your coding agents, built on top of a set of composable skills and some initial instructions that make sure your agent uses them.
 
 
-## We're Hiring!
-
-We're hiring someone to help out full time with Superpowers community and code work. 
-You can read about the job at https://primeradiant.com/jobs/superpowers-community-engineer/
-If this sounds like someone you know, definitely send them our way.
-
 ## Quickstart
 
 Give your agent Superpowers: [Claude Code](#claude-code), [Antigravity](#antigravity), [Codex App](#codex-app), [Codex CLI](#codex-cli), [Cursor](#cursor), [Factory Droid](#factory-droid), [Gemini CLI](#gemini-cli), [GitHub Copilot CLI](#github-copilot-cli), [Kimi Code](#kimi-code), [OpenCode](#opencode), [Pi](#pi).
```
